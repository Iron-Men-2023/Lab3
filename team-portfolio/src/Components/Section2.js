import React from 'react';

function Section2(props) {
    const {title} = props
    return (
        <div className="sect2">
            <h1>{title}</h1>
        </div>
    );
}

export default Section2;